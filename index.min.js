const express=require("express"),app=express(),Datastore=require("nedb");app.listen(3e3,()=>console.log("listening at 3000")),app.use(express.static("public")),app.use(express.json({limit:"1mb"}));const allData=[],database=new Datastore("database.db");database.loadDatabase(),app.post("/api",(request,response)=>{console.log(request.body);const data=request.body,timestamp=Date.now();data.timestamp=timestamp,database.insert(data),console.log(database),response.json({status:"success",timestamp:timestamp,latitude:data.lat,longitude:data.long})});